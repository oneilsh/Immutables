% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-api-flexseq-ends.R
\name{pop_back}
\alias{pop_back}
\title{Pop the Back Element}
\usage{
pop_back(x)
}
\arguments{
\item{x}{A \code{flexseq}.}
}
\value{
A list with fields:
\itemize{
\item \code{element}: the last element, or \code{NULL} when \code{x} is empty.
\item \code{remaining}: the sequence after removing the last element.
}
}
\description{
Returns the last element and the remaining sequence.
}
\details{
This operation is persistent: \code{x} is not modified.

On empty input, returns a non-throwing miss object with
\code{element = NULL} and \code{remaining = x}.
}
\examples{
s <- flexseq("a", "b", "c")
out <- pop_back(s)
out$element
out$remaining
s  # unchanged

pop_back(flexseq())
}
