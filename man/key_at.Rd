% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/50-ordered_sequence-ops.R
\name{key_at}
\alias{key_at}
\title{Key at a Position}
\usage{
key_at(x, index)
}
\arguments{
\item{x}{An \code{ordered_sequence}.}

\item{index}{One-based position to read.}
}
\value{
The key at \code{index}, or \code{NULL} when \code{index} is out of bounds.
}
\description{
Returns the key of the element at a one-based position, without removing it.
This is the positional companion to \code{\link[=peek_at]{peek_at()}} (which returns the value at a
position) and the general form of \code{\link[=min_key]{min_key()}} (\code{key_at(x, 1)}) and
\code{\link[=max_key]{max_key()}} (\code{key_at(x, length(x))}).
}
\details{
Positive integer indices beyond \code{length(x)} return \code{NULL}. Invalid indices
(\code{NA}, non-integer, \verb{<= 0}, or length not equal to 1) error. Pair with
\code{\link[=peek_at]{peek_at()}} to read the value at the same position, or use \code{\link[=pop_at]{pop_at()}} when
you also want the remaining sequence.
}
\examples{
x <- ordered_sequence("a", "b", "c", keys = c(10, 20, 30))
key_at(x, 2)
key_at(x, 10)
}
\seealso{
\code{\link[=peek_at]{peek_at()}}, \code{\link[=min_key]{min_key()}}, \code{\link[=max_key]{max_key()}}, \code{\link[=nearest_key]{nearest_key()}}, \code{\link[=lower_bound]{lower_bound()}}, \code{\link[=pop_at]{pop_at()}}
}
