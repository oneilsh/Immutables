% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/70-plotting.R
\name{plot_structure}
\alias{plot_structure}
\title{Plot the Internal Finger-Tree Structure}
\usage{
plot_structure(
  t1,
  vertex.size = 15,
  vertex.shape = "rounded_rect",
  edge.width = 1,
  label_edges = FALSE,
  title = NULL,
  node_label = "value",
  ...
)
}
\arguments{
\item{t1}{A finger-tree-backed immutables structure.}

\item{vertex.size}{Passed to \code{igraph::plot.igraph}. Ignored by the default
\code{"rounded_rect"} shape, which sizes each box to its own label text.}

\item{vertex.shape}{Vertex shape. Defaults to \code{"rounded_rect"}, a custom
shape registered on first call that auto-sizes every box to fit its
label (so multi-line labels render cleanly without tuning
\code{vertex.size}). Any built-in igraph shape (\code{"circle"}, \code{"rectangle"},
\code{"none"}, ...) also works and is passed through unchanged.}

\item{edge.width}{Edge width passed to \code{igraph::plot.igraph}.}

\item{label_edges}{If \code{TRUE}, draw the child slot name on each edge
(\code{"prefix"}/\code{"middle"}/\code{"suffix"} for \code{Deep} nodes, or the numeric
position for \code{Digit}/\code{Node2}/\code{Node3}).}

\item{title}{Optional plot title.}

\item{node_label}{Either one of the preset modes \code{"value"} (default;
payload for elements, blank for structural), \code{"type"} (node class
name), \code{"both"} (type + value on separate lines), \code{"none"}, or a
user-supplied function \verb{function(node)} returning a single character
string per vertex. The \code{node} argument is a list with fields:
\describe{
\item{\code{id}}{Internal graph vertex id (string).}
\item{\code{type}}{Node class: \code{"Element"}, \code{"Digit"}, \code{"Deep"},
\code{"Node2"}, \code{"Node3"}, \code{"Single"}, or \code{"Empty"}.}
\item{\code{label}}{The default label string.}
\item{\code{measures}}{Named list of accumulated monoid values for the
subtree rooted at this node. For structural nodes these are the
cached values; for element leaves, each entry is the monoid's
\code{measure()} applied to the leaf entry. Keys include built-ins
(\code{.size}, \code{.named_count}, and any structure-specific ones like
\code{.pq_min}) plus any custom name added via \code{\link[=add_monoids]{add_monoids()}}.}
\item{\code{element}}{For element nodes, the raw leaf entry. Shape
depends on the structure type; see \code{\link[=measure_monoid]{measure_monoid()}} for the
entry contract. \code{NULL} for structural nodes.}
}
Measure values are exposed as-is, including list-valued measures
(e.g. the built-in \code{.pq_min} is \code{list(has, priority)}).}

\item{...}{Additional arguments passed to \code{igraph::plot.igraph}.}
}
\value{
Invoked for its side effect (draws to the active graphics
device). Returns \code{NULL} invisibly.
}
\description{
Developer-facing visualizer for the finger-tree backing any immutables
structure (\code{flexseq}, \code{ordered_sequence}, \code{priority_queue},
\code{interval_index}). The S3 \code{plot()} methods on those classes forward to
this function, but \code{plot_structure()} is also callable directly and gives
access to the full \code{node_label} API for custom label formatting. Requires
the \code{igraph} package (listed in \code{Suggests}).
}
\examples{
\dontrun{
t <- as_flexseq(letters[1:8])
plot_structure(t, title = "Finger tree")

# Custom label: show subtree sum at every node (leaves show their own
# value, structural nodes show the accumulated total).
sum_monoid <- measure_monoid(`+`, 0, function(el) el)
xs <- add_monoids(as_flexseq(c(3, 1, 4, 1, 5, 9, 2, 6)),
                  list(sum = sum_monoid))
plot_structure(xs, node_label = function(node) {
  if(node$type == "Element") sprintf("\%g\nΣ=\%g", node$element, node$measures$sum)
  else sprintf("\%s\nΣ=\%g", node$type, node$measures$sum)
})
}
}
\seealso{
\code{\link[=plot.flexseq]{plot.flexseq()}}, \code{\link[=measure_monoid]{measure_monoid()}}, \code{\link[=add_monoids]{add_monoids()}}
}
