% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-api-flexseq-core.R
\name{merge.flexseq}
\alias{merge.flexseq}
\title{Merge Two Sequences}
\usage{
\method{merge}{flexseq}(x, y, ...)
}
\arguments{
\item{x}{A \code{flexseq}.}

\item{y}{A \code{flexseq}.}

\item{...}{Unused.}
}
\value{
A new \code{flexseq}.
}
\description{
Returns a new \code{flexseq} containing all elements of \code{x} followed by all
elements of \code{y}. Thin wrapper over \code{\link[=c]{c()}} for API uniformity across the
package's merge methods; \code{c(x, y)} and \code{merge(x, y)} are equivalent for
\code{flexseq}.
}
\details{
For ordered types (\code{ordered_sequence}, \code{interval_index}), \code{merge()} performs
a proper sorted merge respecting keys/intervals — see \code{\link[=merge.ordered_sequence]{merge.ordered_sequence()}}
and \code{\link[=merge.interval_index]{merge.interval_index()}}. For \code{priority_queue}, see
\code{\link[=merge.priority_queue]{merge.priority_queue()}}.
}
\examples{
x <- flexseq("a", "b")
y <- flexseq("c", "d")
merge(x, y)
}
