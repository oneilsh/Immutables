% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/40-priority_queue-queue-ops.R
\name{pop_min}
\alias{pop_min}
\title{Pop Minimum-Priority Element}
\usage{
pop_min(x)
}
\arguments{
\item{x}{A \code{priority_queue}.}
}
\value{
A list with fields:
\itemize{
\item \code{element}: removed element, or \code{NULL} when \code{x} is empty.
\item \code{priority}: removed priority, or \code{NULL} when \code{x} is empty.
\item \code{remaining}: queue after removal.
}
}
\description{
Removes one minimum-priority element and returns it with the remaining queue.
}
\details{
Ties are stable: when multiple elements share minimum priority, the earliest
element in queue order is removed.
}
\examples{
x <- priority_queue("a", "b", "c", priorities = c(2, 1, 1))
out <- pop_min(x)
out$element
out$priority
out$remaining
pop_min(priority_queue())
}
